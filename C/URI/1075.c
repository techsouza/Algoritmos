#include stdio.h
 
int main() {
 
int num, resto, divisor, contador;
        scanf(%d, &num);
        divisor = 0;
        while(contador = 10000)
        {
          resto = divisor % num;
          if(resto == 2){
                printf(%dn, divisor);
        }
        divisor = divisor + 1;
        contador = contador + 1;
        }


 
    return 0;
}